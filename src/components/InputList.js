import React, { Component } from "react";
import "./style.css";

const InputList = () => {
  return (
    <div>
      <select>
        <option></option>
      </select>
    </div>
  );
};

export default InputList;
